{
  "name": "phonedesk",
  "version": "1.0.0",
  "private": true,
  "description": "PhoneDesk - локальный сервер и веб-интерфейс для запуска приложений с телефона",
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "cross-env NODE_ENV=production npm run build --prefix client && npm run copy-client --prefix server && npm run build --prefix server",
    "start": "cross-env NODE_ENV=production npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
